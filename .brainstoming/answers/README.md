# Brainstorming Answers (Split + Structured)

This folder contains **answers** to the prompt sets in `.brainstoming/ask-*`.

Design goals:
- Mirror the original prompt repositories.
- One answer file per product/template (no giant combined docs).
- Use **only** `source-contents/` as the factual input; anything else is marked as **needs internal data** or **needs external research**.

## Structure

- `ask-for-business-details/`
- `ask-for-customer-commercials/`
- `ask-for-market-research-figures-impact-research-ask/`
- `ask-for-maths-formulas/`
- `ask-from-our-scientists/`

If you want, I can also generate a CSV/Excel “missing data checklist” for Finance/Ops/Sales to fill.
